export interface UserLogin {
  userEmail: string;
  userPassword: string;
}

export interface UserName {
  userName: string;
}

export interface RegisterUser {
  userEmail: string;
  userPassword: string;
}
