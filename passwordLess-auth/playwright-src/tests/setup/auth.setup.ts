// import { test as setup } from '@playwright/test';
// import { imapConfigAdmin, imapConfigUser } from '@_playwright-src/imap-protocol';
// import { LoginPage } from '@_playwright-src/pages/login.pages';
// import { loginAdminViaImap, loginUserViaImap } from '@_playwright-src/test-data/user.data';

// setup('authenticate as admin @admin', async ({ page }) => {
//   await LoginPage.authenticateToApp(page, loginAdminViaImap, imapConfigAdmin);
// });

// setup('authenticate as user', async ({ page }) => {
//   await LoginPage.authenticateToApp(page, loginUserViaImap, imapConfigUser);
// });
